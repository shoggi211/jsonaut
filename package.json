{
  "name": "jsonaut",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-native JSON repair & validation API + MCP server. Free deterministic repair, pay-per-call LLM fallback.",
  "scripts": {
    "dev": "tsx watch src/dev.ts",
    "start": "tsx src/dev.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "hono": "^4.6.14",
    "jsonrepair": "^3.11.2"
  },
  "devDependencies": {
    "@hono/node-server": "^1.13.7",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
